server:
  port: 8090

spring:
  application:
    name: swen-api
  datasource:
    url: jdbc:postgresql://localhost:5432/xanu
    username: postgres
    password: hzandi1989


  jpa:
    database-platform: org.hibernate.spatial.dialect.postgis.PostgisDialect
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: false


minio:
  bucket:
    name: epm1
  default:
    folder: "/"
  access:
    name: minioadmin
    secret: minioadmin

  url: http://127.0.0.1:9000









